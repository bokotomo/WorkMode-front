import React from 'react'

interface Props { }
export const MessageLineBar: React.FC<Props> = props => {
    return (
        <div style={{
            background: "#3e547a",
            height: "1px",
        }} />
    )
}